import StorefrontNotFound from "../../not-found";

/**
 * Same-segment not-found boundary for product pages: notFound() thrown for
 * unknown slugs is caught here, inside the storefront chrome.
 */
export default StorefrontNotFound;
